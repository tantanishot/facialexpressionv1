"use client";

export default function VideoScanLivePage() {
  return (
    <div
      style={{
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
        textAlign: 'center',
        backgroundColor: '#000000', // Optional: Light background for styling
      }}
    >
      <h1>Video Scan Live Page</h1>
      <p>This is the content of the Video Scan Live page.</p>
    </div>
  );
}